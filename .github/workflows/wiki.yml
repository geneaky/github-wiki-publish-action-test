name: Update Wiki

on:
  push:
    branches:
      - master
jobs:
  update-wiki:
    runs-on: ubuntu-latest
    name: Update wiki
    steps:
    - uses: OrlovM/Wiki-Action@v1
      with:
        path: './test'
        token: ${{ secrets.GITHUB_TOKEN }}
